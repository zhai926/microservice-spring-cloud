spring:
  cloud:
    config:
      name:  microservice-config-client   # 需要从github 上获取的资源名称，注意没有yml后缀名
      profile: dev    # 本次访问的配置项
      label: master
      uri: http://config3344.com:3344    # 本次微服务后先去找3344号服务，通过springcloudconfig 获取 github 的服务地址